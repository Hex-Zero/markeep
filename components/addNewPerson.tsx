import * as React from "react";

export interface IAddNewPersonProps {}

export default function AddNewPerson(props: IAddNewPersonProps) {
  return (
    <div>
      <h1>Add New Person</h1>s
    </div>
  );
}
